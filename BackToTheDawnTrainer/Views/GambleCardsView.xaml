<UserControl x:Class="BackToTheDawnTrainer.Views.GambleCardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lex="http://wpflocalizeextension.codeplex.com"
             xmlns:local="clr-namespace:BackToTheDawnTrainer"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BackToTheDawnTrainer.ViewModels"
             d:Background="White"
             d:DataContext="{d:DesignInstance Type=vm:GambleCardsViewModel}"
             d:DesignHeight="100"
             d:DesignWidth="400"
             lex:LocalizeDictionary.DesignCulture="zh-CN"
             lex:ResxLocalizationProvider.DefaultAssembly="BackToTheDawnTrainer"
             lex:ResxLocalizationProvider.DefaultDictionary="Lang"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ContentControl x:Name="_contentControl1"
                        Grid.Row="0"
                        Grid.Column="0"
                        Margin="5,0,0,0" />
        <ContentControl x:Name="_contentControl2"
                        Grid.Row="0"
                        Grid.Column="1"
                        Margin="5,0,0,0" />
        <ContentControl x:Name="_contentControl3"
                        Grid.Row="0"
                        Grid.Column="2"
                        Margin="5,0,0,0" />
    </Grid>
</UserControl>
